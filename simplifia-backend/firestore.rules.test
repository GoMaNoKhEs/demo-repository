rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // 🔥 MODE TEST : Accès complet pour utilisateurs authentifiés
    // ⚠️ À RESTREINDRE EN PRODUCTION !
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
